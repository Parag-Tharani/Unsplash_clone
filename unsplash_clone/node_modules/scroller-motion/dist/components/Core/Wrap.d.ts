import { FC } from 'react';
import { ChildrenRef, DivElementProps, MotionValue } from '../../types';
interface Props extends DivElementProps {
    childrenRef: ChildrenRef;
    disabled?: boolean;
    height: number;
    width: number;
    x: MotionValue;
    y: MotionValue;
}
export declare const Wrap: FC<Props>;
export {};
